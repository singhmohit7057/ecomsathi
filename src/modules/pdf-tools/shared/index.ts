// ============================================================
// PDF Tools — shared (legacy) components
// ============================================================
// ToolPage.tsx is an older layout wrapper used by the root-level
// stubs (MergePDF.tsx, SplitPDF.tsx, etc.) that pre-date the
// pages/ architecture. Those root stubs are kept for reference
// only — App.tsx imports from pages/ exclusively.
// ============================================================
export { default as ToolPage } from './ToolPage'
