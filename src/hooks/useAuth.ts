// Re-export the useAuth hook from AuthContext for convenience.
// Import from here to keep hook imports short:
//   import { useAuth } from '@/hooks/useAuth'

export { useAuth } from '@/contexts/AuthContext'
